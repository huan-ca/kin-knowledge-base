# Jobs

This directory holds human-owned generation inputs.

Each reusable generation workspace should live under `jobs/<job-name>/job.md`.
The generator reads those files and writes derived outputs under `generated/<job-name>/`.
